<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.machanism.machai</groupId>
	<artifactId>gw-mcp-server</artifactId>
	<name>Ghostwriter MCP Server</name>
	<version>1.4.1-SNAPSHOT</version>

	<description>
		Ghostwriter MCP Server combines Ghostwriter and Bindex Core's metadata
		retrieval,
		registration, and library recommendation capabilities with Machai MCP
		Server runtime to expose tools over STDIO or HTTP.
	</description>

	<properties>
		<maven.compiler.release>17</maven.compiler.release>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<gw.model>CodeMie:gpt-5.5-2026-04-24</gw.model>
		<genai.serverId>CodeMie</genai.serverId>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.machanism.machai</groupId>
			<artifactId>ghostwriter</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>org.machanism.machai</groupId>
			<artifactId>bindex-core</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>org.machanism.machai</groupId>
			<artifactId>machai-mcp-server</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<configuration>
					<archive>
						<manifest>
							<mainClass>
								org.machanism.machai.mcp.server.McpServer</mainClass>
						</manifest>
						<manifestEntries>
							<Implementation-Version>${project.version}</Implementation-Version>
						</manifestEntries>
					</archive>
					<descriptors>
						<descriptor>
							src/assembly/assembly-jar.xml</descriptor>
					</descriptors>
					<appendAssemblyId>false</appendAssemblyId>
					<outputDirectory>
						${env.MACHANISM_PACK_DIR}/machai/${project.artifactId}/releases</outputDirectory>
				</configuration>
				<executions>
					<execution>
						<id>build-artifact</id>
						<phase>install</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.machanism.machai</groupId>
				<artifactId>gw-maven-plugin</artifactId>
				<version>${project.version}</version>
			</plugin>
		</plugins>
	</build>

	<licenses>
		<license>
			<name>Apache License, Version 2.0</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<scm>
		<connection>
			scm:git:https://github.com/machanism-org/gw-mcp-server.git</connection>
		<developerConnection>
			scm:git:https://github.com/machanism-org/gw-mcp-server.git</developerConnection>
		<url>https://github.com/machanism-org/gw-mcp-server.git</url>
	</scm>

	<issueManagement>
		<url>https://github.com/machanism-org/gw-mcp-server/issues</url>
	</issueManagement>

</project>
